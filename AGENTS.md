# LOVE 项目开发约束

- 所有产品文案、代码注释与项目文档使用中文；专业标识符可保留英文。
- 场景渲染只消费 `LoveProjectConfig` 的规范化派生结果，禁止直接访问数据库、Cookie 或对象存储密钥。
- 公开分享页只读取不可变发布版本；草稿修改不得直接影响已分享内容。
- 用户图片必须通过服务端校验、方向纠正、去除元信息和 WebP 转码，不保存 data URL 或任意外链。
- 单个代码文件不得超过 1000 行，优先复用迁移后的花园、零件和共享 Three.js 能力。
- 每次功能变更必须运行 `npm run lint`、`npm run typecheck`、`npm test` 与 `npm run build`。
- 涉及数据库时使用独立 `TEST_DATABASE_URL` 运行集成测试，禁止把生产数据库用于测试。
- 涉及页面交互时补充并运行 Playwright E2E；桌面和移动视口都必须验收。
